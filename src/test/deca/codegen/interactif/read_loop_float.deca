// Description :
// Lecture de plusieurs entiers jusqu'a 0

// Resultats :
// Affiche chaque valeur lue


{
    int x;
    x = readFloat();
    while (x != 0) {
        println(x);
        x = readFloat();
    }
}
